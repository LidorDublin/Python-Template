from setuptools import setup

setup(
    name="",
    version="0.1",
    author="Lidor Dublin",
    install_requires=[
        "",
    ],
    extras_require={"dev": ["black", "pylint", "pre-commit"]},
)
